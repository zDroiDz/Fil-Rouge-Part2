package control;

public class ControlSidentifier {

	public boolean Sidentifier(String indentifiant, String motDePasse) {
		
		// Gautier : Je fais le prototype de la fonction
		// Juste pour pouvoir implémenter mon cas de controlIndexation
		// Pas faire gaffe
		
		/*
		 * Note :
		 * 
		 * Plutôt que de faire en sorte que le "contrlIndexation" provoque automatiquement la connexion,
		 * Il vaudrait mieux pas utiliser une fonction qui vérifie que l'utilisateur est déjà connecté
		 * et si ça n'est pas le cas, le connecte ?
		 * 
		 * Faudra qu'on en discute
		 * (venez me voir, j'aurai oublier d'ici là, on va pas se mentir, hein)
		 * 
		 */
		return true;
	}
}
